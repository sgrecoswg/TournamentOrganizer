Test the Tournament Organizer API endpoints using curl against http://localhost:5021.

Run the following checks and report results:

1. `GET /api/players` — list all players
2. `GET /api/events` — list all events
3. `GET /api/leaderboard` — get leaderboard
4. If any endpoint returns an error, report the status code and response body
5. Summarize which endpoints are working and which are not
