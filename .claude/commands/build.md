Build both the .NET backend and Angular frontend. Report any errors.

1. Run `dotnet build` in `src/TournamentOrganizer.Api/`
2. Run `npx ng build` in `tournament-client/`
3. Summarize the results — report any errors or warnings
