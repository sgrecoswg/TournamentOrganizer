Start the .NET API backend server.

1. Run `dotnet run` in `src/TournamentOrganizer.Api/` in the background
2. The API runs at http://localhost:5021
3. Tell the user the server is running and how to access Swagger at http://localhost:5021/swagger
